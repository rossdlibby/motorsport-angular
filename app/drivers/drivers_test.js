'use strict';

describe('myApp.drivers module', function() {
	beforeEach(module('myApp.drivers'));

	describe('drivers controller', function() {

		it('should ....', inject(function($controller) {
			// spec body
			var driversCtrol = $controller('DriversCtrl');
			expect(driversCtrl).toBeDefined();
		}));
	});
});